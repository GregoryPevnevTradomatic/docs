import { DOCX_MIME_TYPE } from '../../constants';

export const TELEGRAM_MESSAGE_ACTION = 'message';

export const SUPPORTED_MIME_TYPES = [
  DOCX_MIME_TYPE,
];

export const SUPPORTED_EXTENSIONS = [
  '.doc',
  '.docx'
];
